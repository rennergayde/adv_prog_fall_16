spot = "dirty"
def cat_clean(catnum):
  if catnum[n] = VOOM:
    spot = "clean"
    return True
  if catnum[n] != VOOM:
      n = n + 1
      return cat_clean[n]
catnum[cat_a, cat_b, cat_c, cat_y, cat_z, VOOM]
cat_clean(1)
